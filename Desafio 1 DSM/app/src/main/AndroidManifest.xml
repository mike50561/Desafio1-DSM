<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Desafio1DSMHA160257"
        tools:targetApi="31">

        <!-- Definir MainActivity como la actividad de entrada -->
        <activity
            android:name=".MainActivity"
            android:exported="true">  <!-- Agregar android:exported -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Otras actividades -->
        <activity
            android:name=".CalcularPromedioActivity"
            android:exported="false" />
        <activity
            android:name=".CalcularSalarioActivity"
            android:exported="false" />
        <activity
            android:name=".CalculadoraBasicaActivity"
            android:exported="false" />
    </application>

</manifest>
